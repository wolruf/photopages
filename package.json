{
  "name": "rgc24.wolruf.xyz",
  "version": "0.0.2",
  "private": true,
  "engines": {
    "npm": "9.x",
    "yarn": "1.x"
  },
  "scripts": {
    "build": "npm install -g thumbsup && thumbsup --config config.json"
  }
}
