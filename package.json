{
  "name": "photography.example.com",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "thumbsup --config config.json"
  },
  "dependencies": {
    "thumbsup": "2.x"
  },
  "engines": {
    "npm": "9.x",
    "yarn": "1.x"
  }
  {
  "scripts": {
    "install:gm": "brew install graphicsmagick || sudo apt-get install -y graphicsmagick || echo 'Please install GraphicsMagick manually'",
    "install:exiftool": "brew install exiftool || sudo apt-get install -y libimage-exiftool-perl || echo 'Please install ExifTool manually'",
    "setup": "npm run install:gm && npm run install:exiftool"
  }
}

}
